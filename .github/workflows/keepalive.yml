name: Keep Streamlit Awake

on:
  schedule:
    # Runs every 25 minutes
    - cron: "*/25 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit
        run: |
          curl -L https://autosplit.streamlit.app/
